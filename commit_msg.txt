fix(calculator): allow operator changing without number input

Fixes a logic flaw in the calculator module where changing an operator back-to-back (e.g. typing `5`, `+`, then `-`) would be ignored. The early return was incorrectly triggered on empty current input, effectively ignoring the new operator selection. This commit rectifies the early return control flow in `calcAction`.
